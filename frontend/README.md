# Frontend

Customer-facing web application (Next.js/React + TypeScript).

No pages, components, or business features exist yet — see OUT OF SCOPE in
the Sprint 1 specification. This package currently contains only tooling
configuration and a placeholder entry point.

Structure (to be populated starting Sprint 2+):
- `src/app/` — routes
- `src/components/` — shared UI components
- `src/lib/` — client utilities
- `src/styles/` — design tokens / global styles
- `public/` — static assets
