# Infrastructure

Local development infrastructure (Docker Compose) and infrastructure-as-code
scaffolding (Terraform). No production infrastructure is provisioned in
Sprint 1 — see OUT OF SCOPE. `infrastructure/terraform/` contains structure
only; no cloud resources are defined or applied.
