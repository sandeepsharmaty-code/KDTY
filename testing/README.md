# Testing

Testing foundation only (frameworks + folder structure). No business/feature
tests exist yet — see Sprint 1.8 (Testing Foundation) and OUT OF SCOPE.

- `unit/` — unit tests (co-located framework config; no suites yet)
- `integration/` — integration tests (no suites yet)
- `e2e/` — end-to-end/UI tests (no suites yet)
